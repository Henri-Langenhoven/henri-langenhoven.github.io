# Adding html to "work notes" i.e. comments

In your work note (the comments under SAFe tickets), surround the html you want to embed with `[code]` tags:
```html
[code]<blockquote>This is a block quote</blockquote>[/code]
```
